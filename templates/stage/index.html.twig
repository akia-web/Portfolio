{% extends 'main/index.html.twig' %}

{% block title %}Stage{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{asset('css/accueil.css')}}">
{% endblock %}

{% block body %}

<h1 class="titre-page">Expérience professionnelle</h1>

<main>
<section>
   
   
   <div class="super-container-projets">
    {% for article in articles %}
  
    <div class="container-projets">
        <img class="image-article-stage" src="{{asset('uploads/images/annonces/' ~ article.images[0].name)}}" alt="">
        <div class="container-infos-projets">
            <h3 class="titre-h3-stage">{{article.title}}</h3>
            {{article.description|raw}}
            <a class="bouton-lien" href="{{path('article', {slug: article.slug})}} ">Lire l'article</a>
        </div>
    </div>
 

    {% endfor %}
    </div>
</section>
        </main>

{% endblock %}
